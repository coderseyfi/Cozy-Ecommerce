@model SearchPanelViewModel
<div class="sidebar white-bg">
    <div class="border-default universal-padding mb-40 sidebar-widget">
        <div class="group-title">
            <h2>Categories</h2>
        </div>
        <ul class="manufactures-list">
            @foreach (var item in Model.Categories)
            {
                <li>
                    <a data-id="@item.Id" data-entity-type="categories">
                        @item.Name
                    </a>
                </li>
            }
        </ul>
    </div>
    <div class="border-default universal-padding mb-40 sidebar-widget">
        <div class="group-title">
            <h2>Availability</h2>
        </div>
        <ul class="manufactures-list">
            <li>
                <input type="checkbox" name="filter.v.availability" value="1" id="Filter-availability-1">
                <label for="Filter-availability-1">In stock (42)</label>
            </li>
            <li>
                <input type="checkbox" name="filter.v.availability" value="0" id="Filter-availability-2">
                <label for="Filter-availability-2">Out of stock (4)</label>
            </li>
        </ul>
    </div>
    <div class="border-default universal-padding mb-40">
        <div class="group-title">
            <h2>Colors</h2>
        </div>
        <ul class="color-option">
            @foreach (var item in Model.Colors)
            {
                <li>
                    <a class="filled" style="background-color:@item.HexCode" data-id="@item.Id" data-entity-type="colors">
                    </a>
                </li>
            }
        </ul>
    </div>
    <div class="border-default universal-padding mb-40">
        <div class="group-title">
            <h2>Filter by Price</h2>
        </div>
        <div id="price-range" data-price-min="@Model.Min" data-price-max="@Model.Max"></div>
    </div>
    <div class="border-default universal-padding mb-40 sidebar-widget">
        <div class="group-title">
            <h2>Brand</h2>
        </div>
        <ul class="manufactures-list">
            @foreach (var item in Model.Brands)
            {
                <li>
                    <a data-id="@item.Id" data-entity-type="brands">
                        @item.Name
                    </a>
                </li>
            }
        </ul>
    </div>
    <div class="border-default universal-padding mb-40 sidebar-widget">
        <div class="group-title">
            <h2>Material</h2>
        </div>
        <ul class="manufactures-list">
            @foreach (var item in Model.Materials)
            {
                <li>
                    <a data-id="@item.Id" data-entity-type="materials">
                        @item.Name
                    </a>
                </li>
            }
        </ul>
    </div>
   
</div>